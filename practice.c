

#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#define size 100 
int main()
{
    char name[size] ; 
    int alp, digit, spec, i ; 
    printf("print a name"); 
    fgets(name, sizeof(size), stdin) ; 
}